from setuptools import find_packages, setup

setup(
    name="src",
    packages=find_packages(),
    version="0.0.1",
    description=
    "This project contains a neural network for Fast Arbitrary Style Transfer in pytorch.",
    author="Theis Friis",
    licens="",
)
